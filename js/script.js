document.addEventListener("DOMContentLoaded", function() {
    
    // Initialize Animate On Scroll (AOS) Library
    AOS.init({
        duration: 800,
        easing: 'ease-in-out',
        once: true,
        mirror: false
    });

});